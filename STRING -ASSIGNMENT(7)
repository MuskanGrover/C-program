//Illustrate the use of strcmpi() function of  string.h header file.
/*The strcmpi() function works similarly to strcmp(), but it ignores the case of the characters when comparing.
 However, keep in mind that its availability and behavior may vary depending on the compiler and platform.*/
 #include <stdio.h>
#include <string.h>

int main() {
    char str1[] = "Hello";
    char str2[] = "hello";

    int result = strcmpi(str1, str2);

    if (result == 0) {
        printf("The strings are equal.\n");
    } else {
        printf("The strings are not equal.\n");
    }

    return 0;
}


