/Display the middle column elements of a 2-D Array of integers.
#include <stdio.h>

int main() {
    int arr[3][4] = {
        {7, 8, 6,5},
        {2, 7, 3,8},
        {4, 7, 9,0},
    };
    int i,j;
    int rs=sizeof(arr)/sizeof (arr[0]);
    printf("\nGENERALISED WAY TO CALCULATE ROW SIZE: %d",rs);
	int cs=sizeof(arr[i])/sizeof (arr[i][0]);
    printf("\nGENERALISED WAY TO CALCULATE COLUMN SIZE: %d",cs);
    int midcolumn=cs/2;


    for(int i=0;i<rs;i++)
    {
    	printf("\nTHE MID COLUMN ELEMENTS  ARE: %d",arr[i][midcolumn]);
	}
	return 0;
}
