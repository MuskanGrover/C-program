// Display the combined count of ‘a’ and ‘A’  in a string entered by the user.
#include<stdio.h>
#include<string.h>

int main()
{
	int count;
	char arr[40]="ARADHYA RATHEE";
	for(int i=0;i<=strlen(arr)-1;i++)
		if(arr[i]=='A' || arr[i]=='a')
	{
		 count++;
	}
	printf("%d",count);

}
